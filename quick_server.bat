@echo off
echo Starting simple web server...
powershell -Command "Start-Process 'http://localhost:8000' ; $listener = New-Object System.Net.HttpListener; $listener.Prefixes.Add('http://localhost:8000/'); $listener.Start(); Write-Host 'Server running at http://localhost:8000'; while($true) { $context = $listener.GetContext(); $request = $context.Request; $response = $context.Response; $path = if($request.Url.AbsolutePath -eq '/') {'index.html'} else {$request.Url.AbsolutePath.TrimStart('/')}; $fullPath = Join-Path $PWD $path; if(Test-Path $fullPath) { $content = [System.IO.File]::ReadAllBytes($fullPath); $response.ContentLength64 = $content.Length; $response.OutputStream.Write($content, 0, $content.Length); } else { $response.StatusCode = 404; } $response.Close(); }"
